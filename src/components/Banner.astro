---

const {title,img,desciption}  = Astro.props

---



<div class="flex bg-gradient-to-br from-sky-200 to-sky-600 h-[60%] w-full text-center justify-center items-center font-Audio">
    <section class=" my-5 w-[50%] flex flex-col justify-center items-center text-center p-5">
        <img src={img} class="w-[30%] mb-4 rounded-full" alt="imagen del Baner">
        <h1 class="mb-2 text-4xl font-bold">{title}</h1>
        <hr class="w-[50%] my-4">
        <h2 class="text-2xl">{desciption}</h2>
    </section>
</div>